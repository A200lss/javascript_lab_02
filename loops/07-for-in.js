// To iterate over elements of arrays
var vowels = ['a','e','i', 'o', 'u']
var lineSkip = '\n'

/*
    Comentario multiples
    lineas para una explicación más detallada
    */

for(var i in vowels)
{
    console.log(`Indice ${i} está la vocal ${vowels[i]} ${lineSkip}`)
}